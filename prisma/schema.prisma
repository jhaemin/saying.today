generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model saying {
  id        Int     @id @default(autoincrement())
  paragraph String
  author    String?
}
